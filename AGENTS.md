# AGENTS Instructions

- When making a commit, first run `git diff` (or equivalent) to see what changed.
- Base the commit message on the diff.
- Use a conventional prefix in the commit message: `feat`, `fix`, `docs`, `chore`, `refactor`, `test`, or `ci`.
